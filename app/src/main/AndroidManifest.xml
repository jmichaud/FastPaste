<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pocketkarma.fastpaste">

    <application
        android:name="com.pocketkarma.fastpaste.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FastPaste">
        <activity
            android:name="com.pocketkarma.fastpaste.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FastPaste.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="com.pocketkarma.fastpaste.CopyToClipboardReceiver" />
    </application>

</manifest>